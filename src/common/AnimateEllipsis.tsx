export const AnimateEllipsis = ({ classes }: { classes?: string }) => {
  return (
    <span class={classes}>
      <span class="one">.</span>
      <span class="two">.</span>
      <span class="three">.</span>
    </span>
  );
};
